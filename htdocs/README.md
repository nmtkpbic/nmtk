This directory contains HTML files - which really is just static media collected by the Django application(s) that
are installed.  Otherwise, this directory will/should be empty (except for this file.)
